# GitHub Configuration
# Get your GitHub token from: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_personal_access_token_here
GITHUB_USERNAME=your_github_username_here

# Obsidian Configuration
# Path to your Obsidian vault folder
OBSIDIAN_VAULT_PATH=D:\Study tracker
# Folder name for daily notes (default: "Calendar")
DAILY_NOTES_FOLDER=Calendar

# Application Configuration
# Number of days to backfill (default: 30)
DAYS_TO_BACKFILL=30
# Maximum commits to show per day (default: 10)
MAX_COMMITS_PER_DAY=10

# GitHub Filtering and Formatting
# Filter out Quartz sync commits (default: true)
FILTER_QUARTZ_SYNC=true
# Make commit messages more human-readable (default: true)
MAKE_COMMITS_READABLE=true
# Keywords to filter out (comma-separated)
QUARTZ_FILTER_KEYWORDS=quartz,sync,update,auto 